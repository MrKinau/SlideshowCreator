<UserControl x:Class="SlideshowCreator.Picture_ContextMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SlideshowCreator"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid x:Name="Picture_Context_menu">
        <Image x:Name="added_Img"  HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Grid.ContextMenu>
            <ContextMenu Background="{DynamicResource SC_BG_COLOR}">
                <MenuItem x:Name="Edit" Header="Edit" Click="Edit_Click" >
                   
                </MenuItem>
            </ContextMenu>
        </Grid.ContextMenu>
    </Grid>
</UserControl>
